version: '3'

services:
  api:
    build: ./api
    ports:
      - "5000:5000"
    environment:
      - REDIS_URL=redis://redis:6379
    networks:
      - red
  react-app:
    build: ./react-front
    ports:
      - "80:80"
    networks:
      - red
  redis:
    image: redis
networks:
  red:
